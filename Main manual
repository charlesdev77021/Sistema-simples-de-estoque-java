Main manual

For now the project is still only executed by the console (terminal) or cmd.
You will have to have the JRE (To run) and if you want to perfect the project, the JDK.
Insert the images into the main folder so that the project opens smoothly, just like the other files;


Por enquanto o projeto ainda só é executado pelo console(terminal)  ou cmd.
VocÊ terá que ter o JRE(Para rodar) e caso queira aperfeiçoar o projeto, o JDK.
Insira as imagens na pasta principal para que o o projeto abra sem problemas, assim como os demais arquivos;
